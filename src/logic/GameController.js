/*
    //drop down

    function test(){

    }

    let now = Date.now();

    function dropDown(now){
        let delta = Date.now() - now;

        if(delta > 1000){
            now = 0;
            requestAnimationFrame(dropDown(now));
        }
    }
*/
